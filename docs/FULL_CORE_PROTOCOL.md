# Former full-core service protocol (superseded)

The ASIC-master FETCH/READ/WRITE service protocol is no longer active. It is
retained by filename for historical links only. LSC-1 accepts one
host-prepared packet and returns one transition response; no autonomous fetch
or external memory controller is planned. The current contract is
[LSC1_PROTOCOL](LSC1_PROTOCOL.md).
